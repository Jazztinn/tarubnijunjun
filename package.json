{
  "name": "tarubnijunjun",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check src/game.js && node --check scripts/build.mjs && node --check scripts/dev-server.mjs && node --check scripts/extract-idle.mjs"
  }
}
